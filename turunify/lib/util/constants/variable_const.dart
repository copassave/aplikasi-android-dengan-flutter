const appName = 'turunify';

const githubUrl =
    'https://github.com/copassave/aplikasi-android-dengan-flutter/tree/main/turunify';

final kToday = DateTime.now();
final kFocusedDay = DateTime.now();
final kFirstDay = DateTime(kToday.year, kToday.month - 3, kToday.day);
final kLastDay = DateTime(kToday.year, kToday.month + 3, kToday.day);

const String metricMeasurement = "KG";
const String imperialMeasurement = "LB";
